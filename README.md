# Password-genrator
The UI was make with the basic concept of HTML and CSS.

The password genrator logics used with javascript.
In this password genrator user genrate password according there need.
if user want Uppercase,lowercase,number,symbol mix password can genrate and also genrate the single unit password also.
